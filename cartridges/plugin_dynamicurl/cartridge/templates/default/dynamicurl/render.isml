<isif condition="${pdict.dynamicURL}">
    <isinclude url="${URLUtils.url(pdict.action, 'cid', pdict.dynamicURL.contentID)}" sf-toolkit="on" />
<iselse />
    <isinclude url="${URLUtils.url(pdict.action)}" sf-toolkit="on" />
</isif>
